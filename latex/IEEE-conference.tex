\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{iftex}
\usepackage[letterpaper, 
left=1.05in, 
right=1.05in, 
top=0.75in, 
bottom=1.07in]{geometry}

\ifPDFTeX
\pdfpagewidth=8.5in
\pdfpageheight=11in
\else
\setlength{\paperwidth}{8.5in}
\setlength{\paperheight}{11in}
\special{papersize=8.5in,11in}
\fi

% --- Added guards ---
\emergencystretch=1.5em
\setlength{\textfloatsep}{18pt plus 2pt minus 2pt}
\setlength{\floatsep}{14pt plus 2pt minus 2pt}
\setlength{\intextsep}{14pt plus 2pt minus 2pt}
\usepackage{balance}
\usepackage{xurl}   % better than url for breaking
\usepackage{url}
\urlstyle{same}
\def\UrlBreaks{%
	\do\/\do-\do\.\do\_\do?\do\&\do=\do:\do\#%
}

\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
		T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}
	
% Preamble (once)
\emergencystretch=2em   % more flexibility to break lines

\raggedbottom

\begin{document}
	
	\title{CM-SolarTS:\\ A Chiang Mai Solar Photovoltaic Power Forecasting Dataset for Temporal Feature Engineering and Time Series Analysis \\
	}
	
	\author{
		\IEEEauthorblockN{Watcharin Sarachai}
		\IEEEauthorblockA{\textit{Information Technology} \\
			\textit{Facuty of Science, Maejo University} \\
			Chiang Mai, Thailand \\
			watcharin\_s@\allowbreak mju.ac.th}
		
		\and
		\IEEEauthorblockN{Chirawan Ronran}
		\IEEEauthorblockA{\textit{Information Technology} \\
			\textit{Facuty of Science, Maejo University} \\
			Chiang Mai, Thailand \\
			chirawan@mju.ac.th}
	}
	
	\maketitle
	
	\begin{abstract}
	Forecasting photovoltaic power is essential for ensuring grid reliability and enabling the smooth integration of renewable energy sources. While prior research emphasizes neural network architecture design, this study introduces a dedicated photovoltaic dataset and systematically examines the impact of temporal feature engineering strategies on forecast accuracy across different model families. We evaluate four encoding strategies—raw baseline, datetime categorical, cyclical, and lag features—using Convolutional Neural Networks (CNN), Long Short-Term Memory (LSTM), and Autoregressive LSTM (AR-LSTM). Results reveal a model-dependent relationship: CNN achieves the highest explanatory power ($\mathbf{R}^{2} \approx 0.85$) but with higher MAE, whereas AR-LSTM with cyclical encoding delivers the most accurate predictions ($\mathbf{MAE} \approx 0.0392$). Interestingly, LSTM and AR-LSTM exhibit negative R² values, highlighting the limitations of this metric for time-series tasks. These findings challenge the assumption that complex feature engineering universally improves performance and provide practical guidelines for selecting encoding strategies based on model architecture and operational objectives.
	\end{abstract}
	
	\begin{IEEEkeywords}
		Photovoltaic Power Dataset, Feature Engineering, CNN, LSTM, ARLSTM
	\end{IEEEkeywords}
	
	\section{Introduction}
	A significant emphasis on photovoltaic (PV) forecasting is important in the new era of energy management, contributing to grid stability and the efficient trade of energy, as well as bringing renewable resources into the energy grid \cite{Ahmed2020}. Solar variability, driven by daily patterns and seasonal changes, makes solar forecasting both a challenge and a necessity \cite{Antonanzas2016}. Uncertain patterns and environmental factors, such as those in Chiang Mai, Thailand, have complicated this challenge across different parts of the world.
	
	Located in the Northern Thailand highlands, the city of Chiang Mai offers a unique case for solar forecasting. The climate conditions in this particular city are affected by monsoons, periodic biomass fires, and haze events, commonly known as the ``smoke season,'' which significantly weaken solar irradiance and the behavior of photovoltaic output \cite{Amnuaylojaroen2020}. As a result, these regional characteristics have not yet been accounted for by globally refined models, thus causing a serious regional data and adaptation gap \cite{Seng2021}. Nonetheless, despite the increasing number of renewable power installations in the region of Chiang Mai, and the pressing need for regionalized solar forecasting, quality regional data on PV installations remains a challenge, at least in the public domain \cite{ASEAN2022}.
	
	Recently, the main focus of forecasting studies has been on the architectural improvements \cite{Wang2022}. Though these methods have achieved state-of-the-art performance, they generally ignore temporal feature learning and fail to account for the effects of encoding methods, architectures, and environmental conditions \cite{Cerqueira2020}. This is particularly important for areas like Chiang Mai, where haze, cloud motions, and topographic conditions produce non-stationary variations that may elicit different responses to the encoding methods \cite{Prasertsan2021}.
	
	Considering these issues, the paper proposes a detailed PV system dataset for the region of Chiang Mai and the implementation of four different time feature encoding methods: raw baseline features, datetime categorical features, cyclical sine and cosine transformations, and lag feature generation under real-world local climate conditions. By experimental verification across several models, the feasibility of effective feature engineering is shown to be highly dependent on the specific model family, in some cases even contradicting the notion that greater feature complexity always translates to higher accuracy, as assumed by \cite{Bommes2021}.
	
	By incorporating methodological sophistication and geographical details, this study offers guidelines for engineering features that simulate solar conditions in Northern Thailand while complementing the debate on adaptive forecasting across different regions.
	
	\section{Literature Review}
	
	\subsection{Geographical Biases in Solar Forecasting Datasets}
	Publicly available datasets for photovoltaic (PV) forecasting exhibit a pronounced geographical imbalance, with the majority originating from temperate regions such as Europe, North America, and China \cite{EuropeanDataset2019, NREL2018, ChinaSolar2020}. This creates a significant research gap for tropical regions like Southeast Asia, where unique environmental factors—most notably the seasonal haze ("smoke season") in Northern Thailand—fundamentally alter solar irradiance and PV generation patterns \cite{Amnuaylojaroen2020, ASEANEnergy2022}. Furthermore, the scarce regional data that does exist often lacks the high temporal resolution or direct AC power measurements required for effective grid integration and trading, limiting the development of locally adapted forecasting models \cite{Seng2021, Ahmed2020}.
	
	\subsection{Methodological Approaches to Solar Forecasting}
	Methodologically, the field has progressed from statistical time-series models to machine learning and, more recently, to complex deep learning architectures \cite{LeCun2015, Hochreiter1997}. However, this drive toward architectural sophistication has often led to treating temporal feature engineering—including techniques such as cyclical encoding and lag feature generation—as a secondary, standardized preprocessing step \cite{Cerqueira2020, BoxJenkins1976}. This oversight is critical, as the optimal method for representing temporal information is not universal but is highly model-dependent \cite{Bommes2021}. This interaction is especially vital for regions with complex, non-stationary weather, where a model's ability to interpret time-based features directly affects its performance. Consequently, the prevalent practice of either selecting a single-feature strategy without justification or optimizing the model architecture in isolation represents a key methodological shortfall \cite{Wang2019}, hindering the development of forecasts that are robust to local climatic challenges.
	
	\section{Methodology}
	
	\subsection{Dataset Description and Preprocessing}
	
	\subsubsection{\textbf{Data Collection and Source Integration}}
	The primary dataset was obtained from a 302.08 kW grid-connected photovoltaic (PV) system at Sansai, Chiang Mai, Thailand ($18.8^\circ$N, $98.9^\circ$E). The dataset spans from November 6, 2021, to October 16, 2025, with a 15-minute temporal resolution, totaling 138,238 time points. Data collection integrates three complementary sources:
	
	\begin{itemize} 
		\item \textbf{On-site Observations}: High-precision sensors provided localized ground-truth measurements of thermal parameters, power generation, and total irradiation, essential for model validation and performance evaluation. 
		\item \textbf{C3S Reanalysis Data}: Atmospheric and land-surface contexts were retrieved from the Copernicus Climate Change Service (C3S) ERA5 and ERA5Land datasets \cite{Copernicus2023, Hersbach2023, CopernicusLand2019, MunozSabater2019}. These records, derived through ECMWF's sophisticated data assimilation, provide physically consistent fields including cloud dynamics, humidity profiles, wind vectors, and radiation fluxes that supplement on-site measurements. 
		\item \textbf{Astronomical Solar Geometry Derivation}: Beyond meteorological inputs from on-site observations and multi-source reanalysis datasets, deterministic astronomical parameters were integrated to provide a robust physical baseline for the sun’s diurnal and seasonal trajectories. These solar geometry features, calculated for the Chiang Mai station, characterize the solar position relative to the local horizon and the PV array (Table \ref{tab:solar_features}).
	\end{itemize}
	
	The integrated feature set comprising 33 variables is detailed in Tables \ref{tab:comprehensive_features} and \ref{tab:solar_features}. By combining high-precision on-site measurements with extensive reanalysis data, this approach ensures reliable PV power predictions across varying meteorological regimes.
		
	\begin{table}[!hb]
		\centering
		\caption{Comprehensive Summary of Input Features}
		\label{tab:comprehensive_features}
		\setlength{\tabcolsep}{1pt}
		\begin{tabular}{|c|c|c|}
			\hline
			\textbf{Variable} & \textbf{Description} & \textbf{Unit} \\ \hline
			
			% --- Section 1 ---
			\multicolumn{3}{|c|}{\textbf{On-site Sensor Measurements (8 Features)}} \\ \hline
			ambient\_temp            & Ambient temperature near PV array                & $^\circ$C \\ \hline
			module\_temp             & Module surface temperature                       & $^\circ$C \\ \hline
			current\_power           & Instantaneous PV power output                    & W \\ \hline
			value\_of\_consumption   & Site load consumption                            & W \\ \hline
			internal\_power\_supply  & Internal power supply (local use)                & W \\ \hline
			grid\_feed\_in           & Power exported to grid                           & W \\ \hline
			external\_energy\_supply & Power imported from grid                         & W \\ \hline
			total\_irradiation       & Global horizontal irradiance (GHI)               & W/m$^{2}$ \\ \hline
			
			% --- Section 2 ---
			\multicolumn{3}{|c|}{\textbf{C3S ERA5 Atmospheric Reanalysis (7 Features)}} \\ \hline
			cc        & Fraction of cloud cover                              & (0--1) \\ \hline
			r         & Relative humidity                                    & \% \\ \hline
			q         & Specific humidity                                    & kg/kg \\ \hline
			t         & Air temperature (pressure level)                     & K \\ \hline
			$T_{2m}$  & 2-metre air temperature                              & K \\ \hline
			u, v      & Eastward / Northward wind (aloft)                    & m/s \\ \hline
			
			% --- Section 3 ---
			\multicolumn{3}{|c|}{\textbf{C3S ERA5-Land Surface Reanalysis (11 Features)}} \\ \hline
			u10, v10  & 10-m wind components (surface)                       & m/s \\ \hline
			sp        & Surface air pressure                                 & Pa \\ \hline
			fal       & Forecast albedo                                      & (0--1) \\ \hline
			slhf      & Surface upward latent heat flux             & J/m$^{2}$ \\ \hline
			sshf      & Surface upward sensible heat flux           & J/m$^{2}$ \\ \hline
			ssr       & Net shortwave radiation at surface          & J/m$^{2}$ \\ \hline
			ssrd      & Downward shortwave radiation                & J/m$^{2}$ \\ \hline
			str       & Net longwave radiation at surface           & J/m$^{2}$ \\ \hline
			strd      & Downward longwave radiation                 & J/m$^{2}$ \\ \hline
			tp        & Total precipitation                         & m \\ \hline
		\end{tabular}
	\end{table}
	
	\begin{table}[!hb]
		\centering
		\caption{Summary of Engineered Astronomical Solar Geometry Features}
		\label{tab:solar_features}
		\setlength{\tabcolsep}{5pt}
		\footnotesize
		\begin{tabular}{|c|c|c|c|}
			\hline
			\textbf{Feature Name} & \textbf{Symbol} & \textbf{Unit} & \textbf{Range} \\ \hline
			Equation of Time      & $EoT$           & min           & $[-14, 16]$ \\ \hline
			Zenith Angle          & $\theta_z$      & deg           & $[0, 180]$ \\ \hline
			Elevation Angle       & $\alpha$        & deg           & $[-90, 90]$ \\ \hline
			Azimuth Angle         & $\gamma_s$      & deg           & $[0, 360]$ \\ \hline
			Declination Angle     & $\delta$        & deg           & $[-23.45, 23.45]$ \\ \hline
			Hour Angle            & $H$             & deg           & $[-180, 180]$ \\ \hline
			Incidence Angle       & $\theta_i$      & deg           & $[0, 90]$ \\ \hline
		\end{tabular}
	\end{table}
	
	\subsubsection{\textbf{C3S Data Structure and Dimensionality}}
	The reanalysis datasets were retrieved in GRIB format, each exhibiting a distinct spatiotemporal structure tailored to its modeling resolution:
	
	\textbf{ERA5 (Atmospheric Reanalysis)}: Operates on a $0.25^\circ\times0.25^\circ$ horizontal grid. The data follows a three-dimensional tensor structure $\mathcal{X} \in \mathbb{R}^{T \times \phi \times \lambda}$, where $T$ represents hourly intervals and $\phi, \lambda$ denote the latitude and longitude grid points surrounding the target location. This format provides a consistent atmospheric profile surrounding the target site.
	
	\textbf{ERA5-Land (Land Surface Reanalysis)}: Offers an enhanced $0.1^\circ \times 0.1^\circ$ resolution with a four-dimensional forecast-based structure $\mathcal{X} \in \mathbb{R}^{D \times S \times \phi \times \lambda}$. Here, $D$ denotes the daily base time, $S$ represents the 24 hourly forecast steps, and $\phi, \lambda$ define the high-resolution spatial grid. This dataset is critical for capturing fine-grained surface parameters and radiation fluxes.
	
	\subsubsection{\textbf{Data Preprocessing Pipeline}}
	Data preprocessing followed a systematic pipeline to ensure quality and consistency across on-site measurements and multidimensional reanalysis data:
	
	\textbf{Spatial Extraction and Weighted Averaging}: Since the geographical coordinates of the PV station do not align perfectly with the fixed grid points of the C3S datasets, a spatial averaging approach was implemented to extract representative atmospheric values. For the target site in Chiang Mai, Thailand, we identified candidate grid points within the immediate vicinity of both ERA5 ($0.25^\circ$ resolution) and ERA5Land ($0.1^\circ$ resolution) datasets. Given the center coordinates of the PV station are $\phi_c=18.8997^\circ\text{N}$ and $\lambda_c=99.0125^\circ\text{E}$, the nearest fixed grid indices $(i, j)_{p}$ are identified by minimizing the spatial displacement:
		\begin{equation*}
			(i,j)_{p} = \arg\min{i, j} \left( |\phi_i - \phi_c|,\allowbreak |\lambda_j - \lambda_{c}| \right)
		\end{equation*}
		
		To incorporate broader spatial context, a local selection window $\mathcal{W}$ was centered on these indices. By expanding the indices within a range of $\pm 1$, a $3\times3$ spatial matrix of candidate points was established, defined by the sets $\mathcal{I}$ and $\mathcal{J}$:
		\begin{equation*}
			\begin{split}
				\mathcal{I} &= { i+k \mid k \in \mathbb{Z}, -1 \leq k \leq 1 } \\
				\mathcal{J} &= { j+k \mid k \in \mathbb{Z}, -1 \leq k \leq 1 }
			\end{split}
		\end{equation*}
		
		The spatial matrix $\mathcal{W}$ is formally defined as the Cartesian product of the index sets, $\mathcal{W} = \mathcal{I} \times \mathcal{J}$, representing a $3 \times 3$ grid of candidate coordinates. For each element $(i, j) \in \mathcal{W}$, a corresponding distance $D_{i,j}$ is computed relative to the station's center $(\phi_c, \lambda_c)$. Specifically, the distance matrix $\mathbf{D} \in \mathbb{R}^{3 \times 3}$ is derived by mapping the geographical coordinates associated with each index pair in the window to their physical displacement:
		\begin{equation*}
			D_{i,j} = f(\phi_i, \lambda_j) \quad \forall (i, j) \in \mathcal{W}
		\end{equation*}
		where $f(\cdot)$ denotes the latitude-corrected Euclidean distance function. By establishing this matrix, the subsequent weighting process can be performed element-wise, ensuring that the influence of each candidate point is strictly determined by its spatial proximity within the defined window.
		
		To transform this gridded data into localized time series, the physical meridional distance $\Delta d_{\phi, i}$ and zonal distance $\Delta d_{\lambda, j}$ (in kilometers) were calculated for each point in the window:
		\begin{equation*}
			\Delta d_{\phi, i} = |\phi_i - \phi_{c}| \times 111.0
		\end{equation*}
		\begin{equation*}
			\Delta d_{\lambda, j} = |\lambda_j - \lambda_{c}| \times 111.0 \times \cos\left(\frac{\phi_i \cdot \pi}{180}\right)
		\end{equation*}
		
		The constant $111.0$ represents the approximate kilometers per degree. The zonal distance incorporates a cosine correction to account for the convergence of meridians near the poles. The total Euclidean distance $D_{i, j}$ is then determined by:
		\begin{equation*}
			D_{i,j} = \sqrt{(\Delta d_{\phi, i})^2 + (\Delta d_{\lambda, j})^2}
		\end{equation*}
		Finally, an \textbf{Inverse Distance Weighting (IDW)} scheme was applied to prioritize grid points closer to the station. Weights $w_{i,j}$ are computed using an exponential decay function to ensure a smooth transition and mitigate discretization artifacts:
		\begin{equation*}
			w_{i,j} = \frac{\exp(D_{max} - D_{i,j})}{\sum_{i,j} \exp(D_{max} - D_{i,j})}
		\end{equation*}
		where $D_{max}$ is the maximum distance within the window.
		
	\textbf{Spatiotemporal Synthesis and Alignment} Following the determination of a single, static weight matrix $\mathbf{w}$ based on the station's geographical coordinates, this matrix is applied uniformly to all atmospheric variables to ensure spatial consistency. The multi-dimensional C3S tensors are transformed into a localized hourly time series through a unified extraction pipeline.
		
		For each discrete hourly timestamp $t_k$, the representative scalar value $V_{v,k}$ for a specific meteorological variable $v$ is synthesized. Using a $3 \times 3$ candidate grid $\mathbf{X}_{v,k}$ centered on the nearest neighbor indices $(i, j)_p$ with an offset of $\pm 1$, the value is calculated via an element-wise multiplication and spatial summation:
		\begin{equation*} 
			V_{v,k} = \sum_{a \in \mathcal{I}} \sum_{b \in \mathcal{J}} (\mathbf{X}{v, k, a, b} \times w_{a,b}) 
		\end{equation*}
		In this context:
		\begin{itemize}
			\item $V_{v,k}$ is the resulting synthesized hourly value for variable $v$ at time $t_k$.
			\item $\mathbf{X}_{v,k,a,b}$ denotes the raw gridded value at spatial index $(a, b)$ for variable $v$ at time $t_k$, extracted using the indices returned by the coordinate search function.
			\item $w_{a,b}$ is the pre-determined IDW weight for index $(a, b)$, calculated from the Euclidean distance $D_{a,b}$ corrected for latitude convergence.
		\end{itemize}
		This unified approach ensures that all extracted features, ranging from cloud cover to wind components, are derived from the same spatial weighting logic, maintaining the physical intercorrelations between different atmospheric parameters. The resulting hourly numerical profile serves as a consistent baseline for subsequent temporal upsampling and predictive modeling.
		
	\textbf{Spatiotemporal Alignment and Upsampling}: To bridge the gap between the synthesized hourly C3S datasets and the high-frequency operational records, a two-stage synchronization process was implemented: (1) \textbf{Temporal Reconstruction and Alignment}: The synthesized meteorological features, derived from the spatial weighted averaging of ERA5 and ERA5Land tensors, were organized into a continuous hourly time series. By directly extracting values from the chronological time coordinates, this stage ensures that all atmospheric variables are precisely aligned with the local observation window, providing a consistent hourly baseline for subsequent processing. (2) \textbf{Temporal Upsampling}: To synchronize the hourly C3S datasets with the 15-minute sampling frequency of the on-site sensors, a \textbf{Cubic Spline Interpolation} was employed. This approach constructs piecewise third-order polynomials, $S_k(t)$, across each hourly interval $[t_k, t_{k+1}]$:
		\begin{equation*}
			S_k(t) = a_k(t - t_k)^3 + b_k(t - t_k)^2 + c_k(t - t_k) + d_k
		\end{equation*}
	By maintaining continuous first and second-order derivatives at each temporal node, this method preserves the physical consistency of volatile variables, such as solar irradiance and ambient temperature, while mitigating the stepwise artifacts inherent in zero-order hold methods. Finally, any interpolated points falling outside the operational timeframe of the on-site records were discarded to ensure strict temporal alignment.
	
	\textbf{Feature Refinement and Dual-stage Scaling}: To ensure numerical stability and optimize training, the dataset underwent a systematic refinement process:
	
	(1) Feature Selection: Demand-side variables were excluded. Since these parameters reflect load consumption patterns rather than meteorological or solar generation drivers, their removal reduces dimensionality and prevents the model from learning irrelevant noise. Consequently, the final feature set used for the forecasting models comprises 29 variables, encompassing on-site measurements, spatially weighted reanalysis data, and deterministic astronomical parameters.
		
	(2) Dual-stage Normalization: Features were harmonized through a two-step scaling pipeline. First, \textit{Standardization (Z-score)} was applied to center the data at a mean of zero. Subsequently, a \textit{Min-Max Scaling} mapped these values to a non-negative $[0, 1]$ range.
			
	\textbf{Data Cleaning and Quality Control}: To ensure dataset integrity, a systematic cleaning process was implemented. Missing values, accounting for less than 0.5\% of the records due to sensor maintenance, were imputed using linear interpolation and forward-filling. Outliers were subsequently identified and removed using the Interquartile Range (IQR) method with a $1.5 \times IQR$ threshold. This combined approach addressed approximately 0.8\% of the total observations, effectively mitigating noise while preserving the natural physical variability inherent in solar generation data.
	
	\subsection{Temporal Feature Engineering Strategies}
	Four distinct temporal feature engineering strategies were implemented to investigate their impact on forecasting performance:
	
	\subsubsection{Strategy 1: Baseline Features (BF)}
	The baseline approach utilizes only the original 29 sensor measurements without any temporal encoding. This serves as the control condition against which other strategies are evaluated. The feature vector at time $t$ is represented as:
	\begin{equation*}
		\mathbf{X}_t^{\text{BF}} = [x_{1,t}, x_{2,t}, \ldots, x_{29,t}]^\top
	\end{equation*}
	where $x_{i,t}$ denotes the $i$-th sensor measurement at time $t$.
	
	\subsubsection{Strategy 2: Datetime Categorical Encoding (DCE)}
	This strategy augments baseline features with one-hot encoded categorical variables:
	\begin{equation*}
		\mathbf{X}_t^{\text{DCE}} = [\mathbf{X}_t^{\text{BF}}, H, M, D, S, I]^\top
	\end{equation*}
	where $H, M, D, S, I$ represent hour, month, day of week, season, and daytime indicator, respectively. These variables capture periodicities across diverse scales, resulting in an expanded feature space of 37 dimensions.
	
	\subsubsection{Strategy 3: Cyclical Encoding (CE)}
	To preserve temporal continuity, time-based variables are transformed into sine and cosine components using $V_{\sin} = \sin(2\pi v/T)$ and $V_{\cos} = \cos(2\pi v/T)$. Here, the pair $(HSin, HCos)$ is derived from the hour ($v \in [0, 23], T=24$), while $(DSin, DCos)$ represents the day of the year ($v \in [1, 365], T=365$). These features are integrated into the augmented input vector:
	\begin{equation*}
		\mathbf{X}_t^{\text{CE}} = [\mathbf{X}_t^{\text{BF}}, HSin, HCos, DSin, DCos]^\top
	\end{equation*}
	This mapping ensures that boundary timestamps such as 23:59 and 00:01 remain numerically adjacent, resolving the discontinuity in the categorical encoding. Consequently, the model better captures smooth, periodic transitions in solar irradiance and seasonal generation patterns.
	
	\subsubsection{Strategy 4: Lag Feature Generation (LFG)}
	This strategy incorporates historical values of key features to capture temporal autocorrelation:
	\begin{equation*}
		\mathbf{X}_t^{\text{LFG}} = [\mathbf{X}_t^{\text{BF}}, \mathbf{X}_{t-1}^{\text{BF}(k)}, \mathbf{X}_{t-24}^{\text{BF}(k)}, \mathbf{X}_{t-96}^{\text{BF}(k)}]^\top
	\end{equation*}
	where $\mathbf{X}_{t-\tau}^{\text{BF}(k)}$ represents a subset of $k$ important features at lag $\tau$. Key features for lag inclusion were selected based on mutual information scores: total irradiance, module temperature, ambient temperature, and DC power output. Lags of 1 (15 minutes), 24 (6 hours), and 96 (24 hours) were chosen to capture short-term, medium-term, and diurnal patterns.
	
	\textbf{Implementation Details:}
	
	\subsection{Model Architectures}
	Three model architectures were implemented to evaluate feature engineering strategies across different learning paradigms. Each architecture is designed to capture temporal dependencies in distinct ways, ranging from convolution-based local pattern extraction to recurrent models for long-range sequence modeling.
	
	First, the one-dimensional Convolutional Neural Network (1D-CNN) focuses on extracting local temporal patterns from sequential data. It processes an input window of 672 time steps with multiple features, which may vary from a baseline of 29 variables to larger sets depending on the encoding strategy. A Conv1D layer with a kernel size of 3 and 128 filters, followed by a ReLU activation, captures short-term dependencies. The resulting feature maps are flattened and passed through a fully connected layer to generate predictions, which are then reshaped to match the required output format (e.g., 96 × 1 for forecasting tasks).
	
	The proposed sequence modeling approach utilizes a Long Short-Term Memory (LSTM) network to capture long-range temporal dependencies. The model processes an input sequence of 672 time steps and outputs the final hidden state, which is passed through a fully connected layer to generate predictions. The output is then reshaped to represent a 24-hour forecast horizon at 15-minute intervals (96 steps). The Dense layer uses a zero-initialized kernel to stabilize training during the initial phases.
	
	The Autoregressive LSTM (AR-LSTM) model is designed for recursive multi-step forecasting, using a feedback mechanism in which each predicted value serves as input to the next time step. The process begins with a warmup phase, in which the historical sequence is encoded to initialize the hidden and cell states and generate the first prediction. Subsequently, a recursive prediction loop iteratively updates the LSTM cell using the previous output as input to produce the next forecast. This approach enables the model to capture temporal dependencies across the entire prediction horizon, with the final output constructed by stacking all predicted steps into a time-feature tensor.
	
	\subsection{Experimental Design}
	The dataset was split chronologically into training (60\%), validation (20\%), and test (20\%) sets to preserve temporal dependencies and include complete seasonal cycles. A sliding window approach was applied, where each sample maps a 7-day historical input (672 time steps) to a 24-hour forecast horizon (96 steps), structured as (Batch, Time, Features) tensors. All models were trained under a consistent configuration: the Nadam optimizer with a learning rate of 0.001, Mean Squared Error loss, a batch size of 32, up to 50 epochs with early stopping (patience = 2), and a dropout rate of 0.3 applied to the CNN and LSTM layers.
	
	\subsection{Implementation Details}
	All experiments were implemented in Python 3.9 using TensorFlow 2.10 for deep learning models and scikit-learn 1.2 for traditional models. Training was conducted on an NVIDIA RTX 3090 GPU with 24GB VRAM. Code reproducibility was ensured by using fixed random seeds (42 for NumPy, TensorFlow, and Python’s random module) and by complete logging of hyperparameters and training metrics.
	
	The complete implementation, including data preprocessing scripts, model definitions, and training pipelines, is publicly available at \url{https://github.com/wsarachai/PhotovoltaicHA}.
	
	
	\section{Results and Discussion}
	
	\subsection{Performance Analysis Across Model Architectures}
	Table \ref{tab:performance_comparison} summarizes the performance of three neural network architectures-CNN, LSTM, and AR-LSTM-across four temporal feature engineering strategies on the 302.08 kW photovoltaic system at Sansai, Chiang Mai.
	
	\begin{table}[!htbp] % discourage top/bottom placement
		\caption{Performance Comparison}
		\label{tab:performance_comparison}
		\centering
		{\small
			\resizebox{\columnwidth}{!}{%
				\begin{tabular}{|c|c|c|c|c|c|c|}
					\hline
					\textbf{Model} & \multicolumn{6}{|c|}{\textbf{Performance Metrics}} \\ 
					\cline{2-7}
					\textbf{} & \textit{Params} & \textit{Strategy} & \textit{MAE} & \textit{RMSE} & $\mathbf{R}^{2}$ & \textit{MAE$_{gain}$} \\
					\hline
					CNN & 8.24M & Baseline & 0.0532 & 0.0901 & \textbf{0.85} & -- \\
					\cline{2-7}
					& 8.24M & Datetime & 0.0609 & 0.0970 & 0.83 & -14.47 \\
					\cline{2-7}
					& 8.24M & Cyclical & 0.0591 & 0.0987 & 0.82 & -11.09 \\
					\cline{2-7}
					& 8.26M & Lag      & 0.0588 & 0.0946 & 0.83 & -10.53 \\
					\hline
					LSTM & 0.44M & Baseline & 0.0408 & 0.0864 & -2.87 & -- \\
					\cline{2-7}
					& 0.54M & Datetime & 0.0408 & 0.0797 & -3.21 & 0.00 \\
					\cline{2-7}
					& 0.60M & Cyclical & 0.0409 & 0.0801 & -3.84 & -0.25 \\
					\cline{2-7}
					& 1.00M & Lag      & 0.0427 & 0.0815 & -8.42 & -4.66 \\
					\hline
					AR & 0.084M & Baseline & 0.0418 & 0.0836 & -2.63 & -- \\
					\cline{2-7}
					LSTM & 0.089M & Datetime & 0.0418 & 0.0837 & -3.65 & 0.00 \\
					\cline{2-7}
					& 0.092M & Cyclical & \textbf{0.0392} & 0.0817 & -4.03 & +6.22 \\
					\cline{2-7}
					& 0.116M & Lag      & 0.0406 & 0.0817 & -8.46 & +2.87 \\
					\hline
				\end{tabular}
		}}
	\end{table}
	
	\subsection{Analysis of Neural Network Performance Across Feature Engineering Strategies}
	The results reveal that temporal feature engineering is strongly architecture-dependent, challenging the assumption that complex encodings universally improve forecasting accuracy.
	\begin{itemize}
		\item \textbf{CNN} achieved the highest explanatory power with an $\mathbf{R}^{2}$ ($\approx0.85$) using baseline features, but its MAE remained higher than sequence-based models. This suggests CNN excels at capturing variance but is less precise in point forecasting.
		\item \textbf{AR-LSTM} delivered the most accurate predictions with cyclical encoding, achieving the lowest MAE ($\approx$0.0392). This indicates that periodic transformations complement its autoregressive design, despite earlier expectations that baseline features would suffice.
		\item \textbf{LSTM} showed minimal improvement across engineered features, with MAE values around 0.0408–0.0427 and consistently negative $\mathbf{R}^{2}$ scores. This underscores the limitations of $\mathbf{R}^{2}$ for time-series tasks and suggests that LSTM’s gating mechanisms inherently capture temporal dependencies without significant benefit from additional encodings.
	\end{itemize}
	Overall, these findings highlight that feature engineering strategies should be tailored to model architecture rather than applied uniformly. CNN benefits from simpler representations for variance explanation, while AR-LSTM gains accuracy from cyclical encoding. LSTM remains relatively insensitive to feature transformations.
	
	\section{Conclusion}
	
	This study evaluated four temporal feature encoding strategies—raw baseline, datetime categorical, cyclical sine/cosine transformations, and lag features—for day-ahead photovoltaic forecasting in Chiang Mai’s haze-affected tropical monsoon climate. Findings confirm that the effectiveness of feature engineering is strongly architecture-dependent. Contrary to earlier assumptions, CNNs did not benefit most from cyclical encoding; instead, they achieved the highest explanatory power ($R^{2} \approx\allowbreak 0.85$) with baseline features, while AR-LSTM delivered the most accurate predictions (MAE $\approx\allowbreak 0.0392$) using cyclical encoding. LSTM models showed minimal improvement and yielded negative R² values, underscoring the limitations of this metric for time-series tasks. These results highlight that complex feature engineering does not universally enhance performance and should be tailored to model architecture. Future work should validate these insights across other tropical regions and benchmark against classical models and Transformer-based architectures. Additionally, the observed degradation of AR-LSTM under certain engineered inputs warrants deeper theoretical investigation.

	
	\balance
	% If the last page still looks cramped, trigger the break earlier:
	% \IEEEtriggeratref{17}  % try a number near where page 6 begins
	
	\bibliographystyle{IEEEtran}
	\bibliography{references}
	
\end{document}
